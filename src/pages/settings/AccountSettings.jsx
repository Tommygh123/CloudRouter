import { FiMail, FiShield, FiUser } from 'react-icons/fi';
import { useAuth } from '../../hooks/useAuth';
import { useTenant } from '../../hooks/useTenant';
export default function AccountSettings(){const{user}=useAuth();const{currentRole}=useTenant();return <div className="space-y-6"><div><p className="text-sm font-semibold uppercase tracking-[.18em] text-blue-600">Settings</p><h1 className="mt-2 text-3xl font-bold text-slate-900">Security & Account</h1><p className="mt-2 text-sm text-slate-500">Your signed-in CloudRouter identity and workspace role.</p></div><section className="rounded-3xl border bg-white p-6 shadow-sm"><div className="grid gap-4 sm:grid-cols-3"><div className="rounded-2xl bg-slate-50 p-4"><FiUser className="text-blue-600"/><p className="mt-3 text-xs uppercase text-slate-500">Name</p><p className="mt-1 font-semibold text-slate-900">{user?.user_metadata?.full_name||'User'}</p></div><div className="rounded-2xl bg-slate-50 p-4"><FiMail className="text-blue-600"/><p className="mt-3 text-xs uppercase text-slate-500">Email</p><p className="mt-1 break-all font-semibold text-slate-900">{user?.email||'—'}</p></div><div className="rounded-2xl bg-slate-50 p-4"><FiShield className="text-blue-600"/><p className="mt-3 text-xs uppercase text-slate-500">Role</p><p className="mt-1 font-semibold text-slate-900">{currentRole?.name||currentRole?.code||'Account'}</p></div></div></section></div>}
