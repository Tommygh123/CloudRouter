import { FiPrinter } from 'react-icons/fi';
import { Link } from 'react-router-dom';
import { useTenant } from '../../hooks/useTenant';
import { TenantLogo } from '../../components/branding/TenantBrand';
export default function VoucherSettings(){const{currentTenant}=useTenant();const name=currentTenant?.business_name||currentTenant?.name||'Your hotspot business';return <div className="space-y-6"><div><p className="text-sm font-semibold uppercase tracking-[.18em] text-blue-600">Settings</p><h1 className="mt-2 text-3xl font-bold text-slate-900">Voucher Settings</h1><p className="mt-2 text-sm text-slate-500">Voucher printing uses your current tenant branding automatically.</p></div><section className="rounded-3xl border bg-white p-6 shadow-sm"><div className="flex items-center gap-3"><TenantLogo tenant={currentTenant} size="lg"/><div><p className="font-bold text-slate-900">{name}</p><p className="text-xs text-slate-500">Internet Voucher • Powered by CloudRouter</p></div></div><p className="mt-5 text-sm leading-6 text-slate-600">Generated voucher printouts include the business name, tenant logo when available, plan, price, validity, username and password. Full passwords remain available only in the newly generated batch window.</p><Link to="/dashboard/hotspot/vouchers" className="mt-5 inline-flex items-center gap-2 rounded-xl bg-blue-600 px-4 py-3 text-sm font-semibold text-white"><FiPrinter/>Open Vouchers</Link></section></div>}
