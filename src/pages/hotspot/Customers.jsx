import SiteGroupedTable from '../../components/operations/SiteGroupedTable';
export default function Customers(){return <SiteGroupedTable title="Customers" description="View hotspot customers and payers grouped by their network site, contact details and activity status." table="hotspot_customers" statusKey="status" columns={[{key:'name',label:'Customer'},{key:'phone',label:'Phone'},{key:'email',label:'Email'},{key:'mac_address',label:'MAC address'},{key:'total_purchases',label:'Purchases'},{key:'total_spent',label:'Total spent',render:v=>`GHS ${Number(v||0).toFixed(2)}`},{key:'last_seen_at',label:'Last seen'},{key:'status',label:'Status',status:true}]} summary={[{label:'Active',value:r=>r.filter(x=>x.status==='active').length},{label:'With phone',value:r=>r.filter(x=>x.phone).length},{label:'Total spend',value:r=>`GHS ${r.reduce((a,x)=>a+Number(x.total_spent||0),0).toFixed(2)}`}]}/>}
